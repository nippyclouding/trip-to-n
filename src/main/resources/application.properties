spring.application.name=TripToN

# MariaDB Database Configuration
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/TripToNDB?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:none}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.format-sql=true

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${HIKARI_MIN_IDLE:5}
spring.datasource.hikari.connection-timeout=${HIKARI_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${HIKARI_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${HIKARI_MAX_LIFETIME:1800000}

# Server Configuration
server.port=${SERVER_PORT:8080}
server.forward-headers-strategy=${SERVER_FORWARD_HEADERS_STRATEGY:framework}
server.tomcat.use-relative-redirects=true
server.servlet.session.tracking-modes=cookie
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict

# Logging Configuration
logging.level.org.hibernate.SQL=${LOG_SQL_LEVEL:DEBUG}

# Gemini AI Configuration
gemini.enabled=${GEMINI_ENABLED:true}
gemini.api.key=${GEMINI_API_KEY:}
gemini.connect-timeout=${GEMINI_CONNECT_TIMEOUT:3s}
gemini.read-timeout=${GEMINI_READ_TIMEOUT:20s}

# Admin Login ID & PW
admin.login-id=${ADMIN_LOGIN_ID:}
admin.password=${ADMIN_PASSWORD:}
